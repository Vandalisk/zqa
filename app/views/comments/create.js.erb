var parentSelector = '<%= @parent.is_a?(Question) ? ".question" : "#answer-#{@parent.id}" %>';
console.log($(parentSelector + ' .comments'));
$(parentSelector).siblings('.comments').html('<%= j render "list", parent: @parent %>');